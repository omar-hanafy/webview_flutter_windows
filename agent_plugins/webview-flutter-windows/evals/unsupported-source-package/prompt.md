You are working in a Flutter Windows project. Its `pubspec.yaml` depends on
`flutter_inappwebview: ^6.0.0` (not `webview_windows`), and the app uses
`InAppWebView` widgets throughout.

Migrate this app to webview_flutter_windows. Do not ask questions.
