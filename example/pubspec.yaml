name: webview_flutter_windows_example
description: Demonstrates how to use the webview_flutter_windows plugin.

publish_to: 'none'

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  webview_flutter_windows:
    path: ../

dev_dependencies:
  ffi: ^2.2.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  win32: ^6.3.0

flutter:
  uses-material-design: true
